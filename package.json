{
  "name": "one-calendar-monorepo",
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "type-check": "turbo type-check",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
