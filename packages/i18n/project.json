{
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm generate"
      }
    },
    "cleanup": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm cleanup"
      }
    }
  }
}