/**
 * Re-exported from @zntr/auth: the template moved into the package when its
 * branding became a parameter, so meet can render mail signed as itself
 * (ADR 0022).
 */
export { renderAuthEmailTemplate } from '@zntr/auth/email-template'
