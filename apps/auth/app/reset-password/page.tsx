import { ResetPasswordForm } from '@/components/auth/reset-form'

/** Password recovery. The only one in the suite (ADR 0021). */
export default function ResetPasswordPage() {
  return <ResetPasswordForm />
}
